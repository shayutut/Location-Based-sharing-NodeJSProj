import { IUser } from './user-model';

export class EventModel {
  title: string;
  date: Date;
  ganer: string;
  location: string;
  publisher: IUser;
}

// let Ganers = ["Board Games", "Pocker", "Jam", "Rock Concert"];
